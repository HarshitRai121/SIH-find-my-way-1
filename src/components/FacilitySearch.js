import React from 'react';

export default function FacilitySearch() {
  return (
    <div>
      <input
        type="text"
        className="border p-2"
        placeholder="Search facilities (e.g., Restrooms, Platforms)"
      />
    </div>
  );
}
